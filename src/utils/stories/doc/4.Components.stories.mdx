import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import { Button } from '../../../components/button/Button.js'

<Meta title="Documentation/4.Components" />

# Basics

There are 2 ways to create components :

- <b>JS Classes</b> : the old way, you won't learn here because it's the old way
- <b>Functional components</b> (also called stateless) : the one you will learn here

## Create component

Let's start with a simple `button` that remember how many times you click it :

<Canvas>
	<Button />
</Canvas>

# Advanced
